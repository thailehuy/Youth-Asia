# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: youth_development
  username: root
  password: 
  encoding: utf8
  host:     localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  database: gb_coconut
  username: gb_coco
  password: gourmet
  encoding: utf8
  host:     localhost

production:
  adapter: mysql
  database: gb_coconut
  username: gb_coco
  password: gourmet
  encoding: utf8
  host:     localhost
